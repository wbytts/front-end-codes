/**
 * 将a和b两个字符串用#拼接
 * @param {string} a
 * @param {string} b
 */
function show(a, b) {
  // @param {参数类型} 参数名
  // 参数类型：这个类型是可以 string 、number、 boolean等基础数据类型！也可以是是个复杂数据类型。
  // a.
  // b.
}

show();

/**
 *
 * @param {Object} person
 * @param {string} person.name 人的姓名
 * @param {number} person.age 人的年龄
 */
function showPerson(person) {
  // person.
}

// 如果不想在每个jsdoc里都写这么多，可以在上方声明一个新的类型

